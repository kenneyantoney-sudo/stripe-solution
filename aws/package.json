{
  "name": "foodnow-stripe-cdk",
  "version": "1.0.0",
  "description": "FoodNow Path B — AWS CDK: API Gateway + Lambda, native Stripe->EventBridge, DynamoDB idempotency, Secrets Manager.",
  "bin": { "foodnow": "bin/app.js" },
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "destroy": "cdk destroy"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "aws-cdk": "^2.150.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.3.0",
    "@aws-sdk/client-secrets-manager": "^3.620.0",
    "@aws-sdk/client-dynamodb": "^3.620.0",
    "@aws-sdk/lib-dynamodb": "^3.620.0"
  }
}
